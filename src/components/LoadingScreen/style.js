import styled from 'styled-components';

export const $LoadingScreen = styled.div`
    img {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
    }
`;
